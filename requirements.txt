# Tradutor de Tela v3 — dependências
# Instale com: pip install -r requirements.txt

mss==9.0.2
Pillow==10.3.0
opencv-python==4.9.0.80
easyocr==1.7.1
deep-translator==1.11.4
numpy==1.26.4
pyinstaller==6.6.0

# ──────────────────────────────────────────────────────────
# IMPORTANTE: EasyOCR com GPU (RTX 4060 Ti)
# ──────────────────────────────────────────────────────────
# O EasyOCR usa PyTorch por baixo. Para ativar CUDA:
#
#   1. Verifique sua versão do CUDA:
#      nvidia-smi
#
#   2. Instale o PyTorch com CUDA (substitua cu121 pela sua versão):
#      pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#
#   3. Então instale o EasyOCR normalmente:
#      pip install easyocr
#
# Se não quiser usar GPU (mais lento mas mais simples):
#   Em captura.py, mude: easyocr.Reader(["en"], gpu=False)
# ──────────────────────────────────────────────────────────
